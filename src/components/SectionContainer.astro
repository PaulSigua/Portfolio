---
const { class : className, id} =  Astro.props
---

<section
    id={id}
    data-section={id}
    class={`section ${className || ""} scroll-m-20 w-full mx-auto max-w-6xl px-4 sm:px-6`}>
    <slot />
</section>